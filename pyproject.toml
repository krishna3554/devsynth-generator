[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "devsynth-generator"
version = "0.1.0"
description = "Synthetic developer conversation generator with validation and export utilities."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0.0",
    "pydantic>=2.7.0",
    "python-dotenv>=1.0.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]
semantic = [
    "sentence-transformers>=3.0.0",
]

[project.scripts]
devsynth-generate = "devsynth_generator.scripts.generate_dataset:main"
devsynth-generate-llm = "devsynth_generator.scripts.generate_llm_dataset:main"
devsynth-validate = "devsynth_generator.scripts.validate_dataset:main"
devsynth-evaluate = "devsynth_generator.scripts.evaluate_dataset:main"
devsynth-split = "devsynth_generator.scripts.split_dataset:main"
devsynth-docs = "devsynth_generator.scripts.generate_docs:main"

[tool.setuptools.packages.find]
include = ["devsynth_generator*"]

[tool.setuptools.package-data]
devsynth_generator = [
    "prompts/*.txt",
    "schemas/*.json",
    "taxonomy/*.json",
    "datasets/*.jsonl",
]
